---
image_resource:
  type: docker-image
  source: { repository: c2cnetworking/dev-postgres }

platform: linux

inputs:
  - name: version
  - name: cf-networking-ci
  - name: cf-networking

run:
  path: cf-networking-ci/tasks/check-cli-version
