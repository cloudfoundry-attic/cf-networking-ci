---
image_resource:
  type: docker-image
  source: { repository: c2cnetworking/deploy }

platform: linux

params:
  ENVIRONMENT: ~

inputs:
  - name: mitre-config

outputs:
  - name: integration-config

run:
  path: sh
  args:
  - -exc
  - |
    cp mitre-config/environments/${ENVIRONMENT}/integration_config.json integration-config/
